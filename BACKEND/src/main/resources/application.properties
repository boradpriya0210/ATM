# Database Configuration
# Default to local MySQL, can be overridden by environment variables
db.url=${DB_URL:jdbc:mysql://localhost:3306/atm_db?useSSL=false&allowPublicKeyRetrieval=true}
db.username=${DB_USERNAME:root}
db.password=${DB_PASSWORD:2210}

server.port=${PORT:21279}
#.\run-backend.bat